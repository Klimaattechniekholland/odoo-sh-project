<odoo>
    <data>

        <record id="action_open_mass_pricing_wizard" model="ir.actions.server">
            <field name="name">Mass Pricing Update</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="binding_model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">
                action = {
                    'type': 'ir.actions.act_window',
                    'name': 'Mass Pricing Update',
                    'res_model': 'product.pricing.mass.wizard',
                    'view_mode': 'form',
                    'target': 'new',
                    'context': {
                        'default_product_ids': [r.id for r in records]
                        }
                    }
            </field>
        </record>

    </data>
</odoo>

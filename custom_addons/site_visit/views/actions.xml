<odoo>
    <data>

        <!-- Action to create Inspection from Template -->
        <record id="action_generate_inspection" model="ir.actions.server">
            <field name="name">Generate Inspection</field>
            <field name="model_id" ref="model_site_visit_template"/>
            <field name="binding_model_id" ref="model_site_visit_template"/>
            <field name="state">code</field>
            <field name="code">
                action = model.action_generate_inspection()
            </field>
        </record>

    </data>
</odoo>

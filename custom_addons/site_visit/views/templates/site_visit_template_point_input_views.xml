<odoo>
    <data>

        <!-- ============================= -->
        <!-- TEMPLATE INPUT VIEWS -->
        <!-- ============================= -->

        <!-- KANBAN TEMPLATE INPUT       -->


        <!-- LIST TEMPLATE INPUT       -->
        <record id="view_site_visit_template_input_form" model="ir.ui.view">
            <field name="name">site.visit.template.input.form</field>
            <field name="model">site.visit.template.input</field>
            <field name="arch" type="xml">
                <form string="Input Field">
                    <header>
                        <button name="open_copy_wizard" type="object" string="Copy Input" class="oe_highlight"/>
                    </header>
                    <sheet><group>
                        <field name="breadcrumb_html" readonly="1"/>
                    </group>
                        <group>
                            <field name="sequence" widget="handle"/>
                            <field name="display_code" class="oe_code_auto_width"/>
                            <field name="name"/>
                            <field name="point_id"/>
                            <field name="field_type"/>
                            <field name="description"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

    </data>
</odoo>

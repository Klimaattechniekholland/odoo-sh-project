<?xml version="1.0"?>
<odoo>
  <record id="email_template_saleorder_custom" model="mail.template">
    <field name="name">Quotation Email (Custom)</field>
    <field name="email_from">${(user.email or '')|safe}</field>
    <field name="subject">Quotation: ${object.name}</field>
    <field name="lang">${object.partner_id.lang}</field>
    <field name="model_id" ref="sale.model_sale_order"/>
    <field name="body_html"><![CDATA[
      <p>Dear ${object.partner_id.name},</p>
      <p>Please find attached our quotation <strong>${object.name}</strong>.</p>
      <p>Quotation total: ${object.amount_total}</p>
    ]]></field>
  </record>

  <record id="email_template_invoice_custom" model="mail.template">
    <field name="name">Invoice Email (Custom)</field>
    <field name="email_from">${(user.email or '')|safe}</field>
    <field name="subject">Invoice: ${object.name}</field>
    <field name="lang">${object.partner_id.lang}</field>
    <field name="model_id" ref="account.model_account_move"/>
    <field name="body_html"><![CDATA[
      <p>Dear ${object.partner_id.name},</p>
      <p>Thank you for your purchase. Invoice <strong>${object.name}</strong> is attached.</p>
      <p>Total: ${object.amount_total}</p>
    ]]></field>
  </record>
</odoo>